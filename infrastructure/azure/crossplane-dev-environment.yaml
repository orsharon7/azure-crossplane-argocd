apiVersion: dev.azure.upbound.io/v1alpha1
kind: DevEnvironment
metadata:
  name: dev-env-${GITHUB_ACTOR}-${GITHUB_EVENT_ISSUE_NUMBER}
  labels:
    developer: "${GITHUB_ACTOR}"
    issue-id: "${GITHUB_EVENT_ISSUE_NUMBER}"
spec:
  parameters:
    region: eastus
    resourceGroup: "env-${GITHUB_ACTOR}-${GITHUB_EVENT_ISSUE_NUMBER}-rg"